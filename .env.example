# Frontend endpoint used by src/App.jsx
# Leave as /api/forms when deploying with the built-in serverless handler.
VITE_FORMS_ENDPOINT=/api/forms

# Supabase auth (required for account sign-up / sign-in)
# VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_PUBLIC_KEY

# Optional frontend rewards sync endpoint override.
# Leave default as /api/rewards when deploying this repo.
# VITE_REWARDS_ENDPOINT=/api/rewards

# Stripe publishable key for embedded checkout UI on the frontend.
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_or_test_key

# Optional frontend token sent with each form submission.
# Only used if FORMS_CHALLENGE_TOKEN is configured on the server.
# VITE_FORMS_CHALLENGE_TOKEN=replace-with-shared-token

# Backend-only environment variable (set in your hosting provider)
# If set, api/forms.js forwards validated submissions to this webhook.
# FORMS_WEBHOOK_URL=https://example.com/forms-webhook

# Comma-separated list of allowed origins for form POSTs.
# Example: FORMS_ALLOWED_ORIGINS=https://velure.co,https://www.velure.co
# FORMS_ALLOWED_ORIGINS=

# Optional server-side challenge token.
# If set, requests must include matching challengeToken.
# FORMS_CHALLENGE_TOKEN=replace-with-shared-token

# Optional webhook signing secret.
# If set, api/forms.js sends X-Velure-Signature and X-Velure-Timestamp headers.
# FORMS_WEBHOOK_SECRET=replace-with-webhook-secret

# Stripe checkout configuration used by api/checkout.js.
# STRIPE_SECRET_KEY=sk_live_or_test_key

# Optional checkout origin allowlist (fallbacks to FORMS_ALLOWED_ORIGINS if not set).
# CHECKOUT_ALLOWED_ORIGINS=https://velure.co,https://www.velure.co

# Optional canonical site origin for Stripe success/cancel URLs.
# PUBLIC_SITE_URL=https://your-project.vercel.app

# Required for /api/rewards cross-device sync.
# SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_PUBLIC_KEY
# SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY

# Optional rewards origin allowlist (fallbacks to checkout/forms allowlist).
# REWARDS_ALLOWED_ORIGINS=https://velure.co,https://www.velure.co
